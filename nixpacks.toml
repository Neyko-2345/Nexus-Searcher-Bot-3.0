[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[start]
cmd = "node --experimental-sqlite artifacts/discord-bot/src/index.js"
